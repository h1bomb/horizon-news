# Environment variables for the Horizon CMS worker.
# Copy to .env and fill in values. Worker scripts (pnpm worker) read these.
# Variables are added by later tasks as worker features are implemented.
